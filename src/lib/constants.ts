import type { Locale } from "@/types";

export const locales: Locale[] = ["en", "ta", "fr"];
export const defaultLocale: Locale = "en";

export const localeLabels: Record<Locale, string> = {
  en: "English",
  ta: "தமிழ்",
  fr: "Français",
};

export const appName = "Kalvikoodam";
