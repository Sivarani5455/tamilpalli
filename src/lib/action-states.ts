import type { AuthState } from "@/types";

export const initialAuthState: AuthState = {
  ok: false,
  message: "",
};

export const initialCrudState: AuthState = {
  ok: false,
  message: "",
};

export const initialGameState: AuthState = {
  ok: false,
  message: "",
};
